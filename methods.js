/**
 * 1- beforeAll(fn,timeout) run before all tests
 * 2- beforeEach(fn,timeout) run before each test
 * 3- afterAll(fn,timeout) run after all tests
 * 4- afterEach(fn,timeout) run each all test
 */

beforeAll(()=>{
    // Examples
    // 1- Connect To DB
    // 2- Empty Testing Table
    // 3- Add Dummy Data For Testing
    // 4- Prepare Everything Before Testing
})


afterAll(()=>{
    // Examples
    // 1- Disconnect From DB
    // 2- Clean Cache
    // 3- Clean Db
})